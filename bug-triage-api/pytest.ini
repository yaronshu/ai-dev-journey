[pytest]
# Add the project root (this folder) to sys.path so `import app...` works
# no matter which sub-folder the tests live in.
pythonpath = .

# Where to look for tests.
testpaths = tests

# Run async tests (the @pytest.mark.asyncio ones) without extra setup.
asyncio_mode = auto
